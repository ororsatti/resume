{
    "name": "resume",
    "version": "1.0.0",
    "scripts": {
        "start": "browser-sync start -s --index ./index.html --files \"./data\""
    },
    "devDependencies": {
        "browser-sync": "^2.27.10"
    }
}
