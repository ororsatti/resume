{
    "name": "resume",
    "version": "1.0.0",
    "scripts": {
        "start": "browser-sync start --server --files \"index.html, app.js, resume.json, index.css\""
    },
    "devDependencies": {
        "browser-sync": "^2.27.10"
    }
}
